[package]
name = "invoices-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "invoices core: invoicing domain — money math, VAT, numbering"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
